<!DOCTYPE html>
<html lang="en">
  <head>
    <style>
      body {
        background-image: url('/images/background3.jpg');
        background-repeat: no-repeat;
        background-size: cover;
        min-height: 100%;
      }
    </style>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Phones</title>
    <%- include('partials/_head') %>
  </head>
  <body>
    <%- include('partials/_navbar', {title: 'Category'}) %>
    <div class="container p-3">
      <div class="display-5 text-white mb-3"><%= category.toUpperCase() %></div>
      <div class="row">
        <%itemList.map((item, index)=> { %>
        <!-- <div class="container<%= index+1%>">
          <img src="<%= item.img %>" width="193" height="300" />
          <br />
          <label id="label1" class="ml-4 my-2"> <%= item.name%> </label>
          <br />
          <button
            onclick="location.href='/item/<%=item._id%>' "
            id="<%=item.name%>"
            class="btn btn-secondary ml-3"
          >
            View
          </button>
        </div> -->
        <div class="col-md-3 col-sm-6 m-2">
          <div class="card text-center" style="max-width: 16rem">
            <img
              src="<%=item.img%>"
              class="card-img-top"
              alt="<%= item.name%>"
              height="400"
            />
            <div class="card-body">
              <h5 class="card-title"><%= item.name %></h5>
              <button
                onclick="location.href='/item/<%=item._id%>';"
                id="<%= item.name%>"
                class="btn btn-success ml-auto w-100 mt-2"
              >
                View
              </button>
            </div>
          </div>
        </div>
        <% });%>
      </div>
    </div>
  </body>
</html>
